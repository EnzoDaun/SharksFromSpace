export enum OpenAIUserPartType {
  TEXT = 'text',
  IMAGE_URL = 'image_url',
  IMAGE_FILE = 'image_file',
}

export enum OpenAIAssistantPartType {
  TEXT = 'text',
  IMAGE_URL = 'image_url',
  IMAGE_FILE = 'image_file',
}